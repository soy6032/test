server:
  port: 8090
spring:
  datasource:
    url: jdbc:mysql://175.125.92.59:3306/bitdb?characterEncoding=utf8
    username: bit
    password: bit!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace